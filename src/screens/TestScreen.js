import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const TestScreen = () => {
    return(
        <View>
            <Text>This is the TestScreen page</Text>
        </View> 
    );
};

const styles = StyleSheet.create({});

export default TestScreen;